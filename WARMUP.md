# Warmup

1. Was genau macht unser Router mit einer URL wie `/produkte/7`, Schritt für Schritt, vom Request bis zum antwortenden Code?

2. Warum verhindert ein Prepared Statement SQL-Injection, ein zusammengeklebter SQL-String aber nicht?

3. Wo liegt in unserem Projekt die n:m-Beziehung und warum braucht sie eine Zwischentabelle?

4. Was gibt `array_first()` zurück, wenn das Array leer ist, und warum ist das besser als `$arr[0]`?

5. Welche Zeile bzw. welchen Teil meines Projektes würde ich einem Interviewer zuerst zeigen und warum?
